# For running the server directly on your host with `npm run server`
# (against the Postgres container started via `npm run db:up`). Copy to
# apps/server/.env. Match POSTGRES_* to the root .env if you changed them.
DATABASE_URL=postgresql://fitquest:fitquest@localhost:5432/fitquest
JWT_SECRET=dev-secret-change-me
PORT=3000
